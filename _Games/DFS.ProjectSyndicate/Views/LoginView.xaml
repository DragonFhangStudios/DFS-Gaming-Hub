<UserControl x:Class="DFS.ProjectSyndicate.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DFS.ProjectSyndicate.ViewModels">

    <UserControl.DataContext>
        <vm:LoginViewModel />
    </UserControl.DataContext>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Style="{StaticResource CardStyle}" Width="400">
            <StackPanel Margin="20">
                <TextBlock Text="🔐 Login to Syndicate" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>

                <TextBlock Text="Select User" Style="{StaticResource SubHeaderTextStyle}"/>

                <ItemsControl ItemsSource="{Binding Players}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}"
                                    Click="LoginButton_Click"
                                    Tag="{Binding}"
                                    Height="45"
                                    Margin="0 0 0 10"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Enter Passcode:" Margin="0 20 0 5"/>
                <PasswordBox Name="PasswordInput" Height="35" VerticalContentAlignment="Center" Padding="5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
