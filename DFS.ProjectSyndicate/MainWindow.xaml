<Window x:Class="ProjectSyndicate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ProjectSyndicate.Views"
        Title="Project Syndicate" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <StackPanel Grid.Column="0" Background="#222" Margin="10">
            <Button Content="Dashboard" Click="Dashboard_Click" Margin="5"/>
            <Button Content="Jobs" Click="JobsTab_Click" Margin="5"/>
            <Button Content="🔒 Crimes (Coming Soon)" IsEnabled="False" ToolTip="Criminal underworld not yet accessible" Opacity="0.5" Margin="5"/>
        </StackPanel>

        <!-- Main Content Area -->
        <ContentControl Grid.Column="1" x:Name="MainContent"/>
    </Grid>
</Window>
