<UserControl x:Class="DFS.ProjectSyndicate.Views.CrimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DFS.ProjectSyndicate.ViewModels"
             x:Name="CrimeRoot">
    <UserControl.DataContext>
        <vm:CrimeViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Crime Center" Style="{StaticResource HeaderTextStyle}"/>

            <ItemsControl ItemsSource="{Binding Crimes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Attempt" Click="AttemptCrime_Click" Tag="{Binding}" HorizontalAlignment="Left" Width="100"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{Binding LastResult}" FontSize="14" FontWeight="Bold" Margin="0 10 0 0" Foreground="{StaticResource SuccessBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
